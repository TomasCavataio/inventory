version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  build:
    commands:
      - npm -w apps/web run build

artifacts:
  base-directory: apps/web/dist
  files:
    - '**/*'
