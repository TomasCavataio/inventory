version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  build:
    commands:
      - npm -w apps/web run build
  post_build:
    commands:
      - aws s3 sync apps/web/dist/ s3://inventory-tomas-frontend-eunorth1-2026/ --delete
